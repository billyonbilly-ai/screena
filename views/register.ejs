<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/register.css">
    <title>Document</title>
</head>
<body>
    <h1>Register</h1>

    

    <form action="/users/register" method="POST">

        
    <ul>
        <% if(typeof errors != 'undefined') { %> <% errors.forEach(error=> { %>
                <li class="alert"><%= error.message %></li>

           <% }) %> <% } %>
    </ul>

        <div>
            <input type="text" id="name" name="name" placeholder="Name" class="login login-email" required />
        </div>

        <div>
            <input type="email" id="email" name="email" placeholder="Email" class="login login-email" required />
        </div>
        <div>
            <input type="password" id="password"class="login login-email" name="password" placeholder="Password" required />
        </div>
        <div>
            <input type="password" id="password2" class="login login-password" name="password2" placeholder="Confirm password" required />
        </div>

        <div>
            <input type="submit" value="Register" class="submit-btn" />
        </div>
        <a href="/users/login"><p>Already registered? Login here</p></a>
    </form>
</body>
</html>